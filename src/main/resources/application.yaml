springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui-custom.html
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: food_payment
  application:
    name: Good Food Payment Service
server:
  port: 8080

